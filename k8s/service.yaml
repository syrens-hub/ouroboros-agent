apiVersion: v1
kind: Service
metadata:
  name: ouroboros-agent
  namespace: ouroboros
spec:
  selector:
    app: ouroboros-agent
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
